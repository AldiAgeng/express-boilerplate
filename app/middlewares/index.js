const authorization = require("./authorization/authorization");
const bodyValidation = require("./validations/body.validation");
const checkBody = require("./validations/check.body");

module.exports = {
  authorization,
  bodyValidation,
  checkBody,
};